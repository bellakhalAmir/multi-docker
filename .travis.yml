sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t amirbellakhal/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true amirbellakhal/react-test npm test

after_success:
  - docker build -t amirbellakhal/multi-client ./client
  - docker build -t amirbellakhal/multi-nginx ./nginx
  - docker build -t amirbellakhal/multi-server ./server
  - docker build -t amirbellakhal/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push amirbellakhal/multi-client
  - docker push amirbellakhal/multi-nginx
  - docker push amirbellakhal/multi-server
  - docker push amirbellakhal/multi-worker
